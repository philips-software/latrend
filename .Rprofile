options(cluslong.time='Assessment')
options(cluslong.id='Subject')

library(stats)
library(utils)
library(data.table)
library(magrittr)
if(require(cluslong)) {
  data('testLongData')
  dt = testLongData
  clm = clMethodKML(formula=Measurement ~ 1)
}
