# Test whether the resulting lcModel supports prediction through predictForCluster() and predict()
# * predictForCluster(newdata = NULL)
# * predict(newdata = NULL)
# * predictForCluster(newdata = model.data())
# * predict(newdata = model.data())
####
